{
  "name": "alert-npm-publish",
  "version": "1.0.0",
  "description": "alert when npm pkg is published",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-cron": "^2.0.3"
  }
}
